<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parkvina.fakejumping.mapper.StoreMapper">

    <insert id="insertStore"
            parameterType="com.parkvina.fakejumping.entity.Store"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO store (name, region, city,district,address,is_active)
        VALUES (#{name}, #{region}, #{city},#{district},#{address},#{isActive})

    </insert>
    <select id="findStores" resultType="com.parkvina.fakejumping.entity.Store">
        SELECT *
        FROM store

        <if test="region != null and region != ''">
            AND region = #{region}
        </if>

        <if test="city != null and city != ''">
            AND city = #{city}
        </if>

        <if test="city == null or city == ''">
            AND city IS NULL
        </if>

        <if test="district != null and district != ''">
            AND district = #{district}
        </if>

        ORDER BY id DESC
    </select>
    <select id="selectActiveStore"
            resultType="com.parkvina.fakejumping.entity.Store"
    >
        SELECT *
        FROM store
        WHERE is_active = 1

    </select>
    <select id="countStoreSummary" resultType="map">
        SELECT
            COUNT(*) AS total,
            SUM(CASE WHEN is_active = 1 THEN 1 ELSE 0 END) AS active,
            SUM(CASE WHEN is_active = 0 THEN 1 ELSE 0 END) AS inactive
        FROM store
    </select>
    <select id="findDistricts" resultType="string">
        SELECT DISTINCT district
        FROM store
        WHERE region = #{region}
        AND district IS NOT NULL

        <if test="city != null and city != ''">
            AND city = #{city}
        </if>

        <if test="city == null or city == ''">
            AND city IS NULL
        </if>

        ORDER BY district
    </select>
    <select id="regionSummary" resultType="com.parkvina.fakejumping.dto.RegionSummary">
        SELECT region, COUNT(*) AS count
        FROM store
        GROUP BY region
        ORDER BY count DESC
    </select>
    <select id="findPendingStoreInfo" resultType="com.parkvina.fakejumping.dto.PendingStoreInfo">
SELECT id,created_at AS createdAt,region,name from store where is_active=1 AND open_at IS NULL ORDER BY created_at DESC
<if test="limit!=null">LIMIT #{limit}</if>

    </select>
</mapper>